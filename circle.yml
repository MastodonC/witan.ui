deployment:
  production:
    tag: /release-.*/
    commands:
      - ./build_prod.sh
      - lein upload-data witan-ui-production
  staging:
    branch: master
    commands:
      - ./build_prod.sh
      - lein upload-data witan-ui-staging
